const add = (int) => {
    return (2+2);
}

const subtract = (int) => {
    return (2-2);
}

const divide = (int) => {
    return (2/2);
}

const multiply = (int) => {
    return (2*2);
}

const sqroot = (int) => {
    return Math.sqrt(9);
}
const max = (int) => {
    return Math.max(2, 6);
}

module.exports = {
    add,
    subtract,
    divide,
    multiply,
    sqroot,
    max
}